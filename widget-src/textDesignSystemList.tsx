const { widget } = figma;
const { AutoLayout, Text, useSyncedState, Input, SVG } = widget;

const TextDesignList = () => {

    return <AutoLayout>
        <Text>TextDesignList</Text>
    </AutoLayout>
}

export default TextDesignList