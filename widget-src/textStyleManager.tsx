const { widget } = figma;
const { AutoLayout, Text, useSyncedState, Input, SVG } = widget;

const TextStyleManager = () => {

    return <AutoLayout>
        <Text>TextStyleManager</Text>
    </AutoLayout>
}

export default TextStyleManager